# Prefer pnpm (see pnpm-lock.yaml). If using npm, allow install despite peer conflicts.
legacy-peer-deps=true
# Hoist so Next.js is not resolved under a path that includes @playwright (avoids React Client Manifest / next-devtools error).
public-hoist-pattern[]=*next*
public-hoist-pattern[]=*react*
public-hoist-pattern[]=*react-dom*
