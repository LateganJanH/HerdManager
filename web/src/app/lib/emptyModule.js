/**
 * Empty module used to stub optional/peer dependencies (e.g. Playwright) so they
 * are never bundled into the client. Prevents ChunkLoadError and "reading 'call' of undefined".
 */
module.exports = {};
